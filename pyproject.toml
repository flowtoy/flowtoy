[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flow"
version = "0.1.0"
description = "Minimal local-first flow runner prototype"
dependencies = []
